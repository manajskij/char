<%= form_for(@problem) do |f| %>
    <%= render 'shared/error_messages', object: f.object %>
    <div class="field">
      <%= f.cktext_area :content, placeholder: "Compose new micropost..." %>
    </div>
    <%= f.submit "Создать!", class: "btn btn-large btn-primary" %>
<% end %>